<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.wayde.a3055scouting.MainActivity">

    <TextView
        android:text="Scouting Type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:id="@+id/mainScreenTitle"
        tools:text="Scouting Type"
        android:textAlignment="center"
        android:textSize="18sp" />

    <Button
        android:text="Robot / Competition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/botNavButton"
        android:layout_below="@+id/pitNavButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textStyle="normal|italic"
        android:textAlignment="center"
        android:fontFamily="sans-serif-medium" />

    <Button
        android:text="Pit Scouting"
        android:layout_width="match_parent"
        android:layout_marginTop="18dp"
        android:id="@+id/pitNavButton"
        android:elevation="3dp"
        tools:text="Pit Scouting"
        android:layout_height="wrap_content"
        android:layout_below="@+id/mainScreenTitle"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ImageView
        android:contentDescription="3055Logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/logo"
        android:layout_below="@+id/botNavButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="27dp"
        android:id="@+id/imageView2" />

</RelativeLayout>
